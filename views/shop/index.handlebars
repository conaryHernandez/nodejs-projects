<main>
  <h1>Welcome to my store
    {{#>myName name="Conary"}}
      Testing partials
    {{/myName}}
  </h1>
  <p>List of all the products (Aprox.{{calculation 50}})</p>
  <div class="grid">
    {{#if hasProducts }}
      {{#each prods}}
        <article class="card product-item">
          <header class="card__header">
            <h1 class="product__title">{{ this.title }}</h1>
          </header>
          <div class="card__image">
            <img src="/{{ this.imageUrl }}" alt="{{ this.title }}">
          </div>
          <div class="card__content">
            <h2 class="product__price">${{ this.price }}</h2>
            <p class="product__description">{{ this.description }}</p>
          </div>
          <div class="card__actions">
            <a href="/products/{{this._id}}" class="btn">Details</a>
            {{#if ../isAuthenticated}}
                <form action="/cart" method="POST">
                    <input type="hidden" name="_csrf" value="{{../csrfToken}}">
                    <button class="btn">Add to Cart</button>
                    <input type="hidden" name="productId" value="{{ this._id }}"/>
                </form>
            {{/if}}
          </div>
        </article>
      {{/each}}
<<<<<<< HEAD
      {{#>pagination}}
        Please wait
      {{/pagination}}
=======
      <section class="pagination">
        {{#if isNotFirst}}
          {{#if previousIsNotFirst}}
            <a href="/?page=1">1</a>
          {{/if}}
        {{/if}}
        {{#if hasPreviousPage}}
            <a href="/?page={{previousPage}}">{{previousPage}}</a>
        {{/if}}
        <a href="/?page={{currentPage}}">{{currentPage}}</a>
        {{#if hasNextPage}}
          <a href="/?page={{nextPage}}">{{nextPage}}</a>
        {{/if}}
        {{#if isNotLastPage}}
          {{#if nextIsNotLast}}
            <a href="/?page={{lastPage}}">{{lastPage}}</a>
          {{/if}}
        {{/if}}
      </section>
>>>>>>> ff23cad92eadc2472a4c69334744b2782622dfd4
    {{ else }}
      <h1>No products found</h1>
    {{/if }}
  </div>
</main>
